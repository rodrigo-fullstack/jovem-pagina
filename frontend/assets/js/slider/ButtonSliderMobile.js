import ButtonSlider from "./ButtonSlider.js";

export default class ButtonSliderMobile extends ButtonSlider{
    constructor (element, name){
        super(element, name);
    }

    addEventListener(slider){
        // super(slider);
    }

    
}